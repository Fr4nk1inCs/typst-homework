#import "homework-comp.typ": *

#show: project.with(
    course: "课程作业",
    number: 0,
    name: "姓名",
    id: "PB2XXXXXXX",
    code_with_line_number: true,
)

#question(1)[
    #lorem(60)
    $ i ħ (diff)/(diff t) Phi(arrow(r), t) = 
    [- ħ/(2m) nabla^2 + V(arrow(r))] Phi(arrow(r), t) $
    ```rust
    fn main() {
        println!("Hello, world!");
    }
    ```
][
#lorem(10)

]



#lorem(1000)


#lorem(10)

$ i ħ (diff)/(diff t) Phi(arrow(r), t) = 
[- ħ/(2m) nabla^2 + V(arrow(r))] Phi(arrow(r), t) $

```rust
fn main() {
    println!("Hello, world!");
}
```

```rust
fn main() {
    println!("Hello, world!");
}
```
